# Exploratory Data Analysis

## Picking up the preprocessing thread
In the last chapter we used dplyr to reshape and clean data. That work continues here to expand on a couple of additional ideas.

Cleaning is only half of this chapter. Once our data are tidy, the same dplyr tools that helped fixed it can also assist us in understanding it. **Exploratory data analysis (EDA)** is the practice of examining data to learn what it contains and what questions it raises, before any formal modeling begins. Good EDA is where a data professional uses intuition and methodical inspection to spots surprises and decide what is worth investigating further.

## A short statistics refresher
EDA leans on a handful of basic statistical ideas. A quick review is presented here that is enough to follow the chapter.

[#WORKING DRAFT BELOW HERE#]
### Measures of central tendency
A **measure of central tendency** describes the "typical" value in a set of numbers. Three are common:

- The **mean** is the arithmetic average — add the values and divide by how many there are. It is the most familiar measure, but it is sensitive to extreme values.
- The **median** is the middle value when the numbers are sorted. Half fall below it and half above. Because it ignores how far away the extremes are, it resists their pull.
- The **mode** is the value that appears most often. It is the only one that works for categories (the most common major, say), not just numbers.

The gap between the mean and the median is itself informative. When they are close, the data is roughly balanced. When the mean is pulled well above the median, a few large values are stretching it upward — exactly what we saw with institutional completion counts in the last chapter.

### Measures of spread
Central tendency tells us where the middle is; **spread** tells us how far the values scatter around it.

- The **range** is the distance from the smallest value to the largest. Simple, but driven entirely by the two extremes.
- The **interquartile range (IQR)** is the range of the middle half of the data — the distance from the 25th to the 75th percentile. It describes the bulk of the values while ignoring the extremes.
- The **standard deviation (SD)** measures the typical distance of values from the mean. A small SD means values cluster tightly; a large one means they are widely dispersed.